{
  "name": "bootstrapstudio-export",
  "version": "0.0.1",
  "description": "Bootstrapstudio export script for different job portals",
  "scripts": {
    "export": "rm -rf /app/stepstone && parcel build --no-cache --out-file ../index.html --out-dir /app/stepstone/images --public-url images export/test/index.html && mv /app/stepstone/images/index.html /app/stepstone/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel-bundler": "^1.6.2",
    "posthtml": "^0.11.3",
    "posthtml-remove-tags": "^0.1.0"
  }
}
